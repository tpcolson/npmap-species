# Layer Re-order Buttons

These buttons at the top-left of each color legend row (::) allow the user to re-order the data layers on the map (eg bring the orange layer to the front).

### Help statement
